version: 1
backend:
  phases:
    preBuild:
      commands:
        - yum install jq -y
        # - cat amplify/team-provider-info.json | jq '.["master"]["categories"]["function"]["smartshelf72d32494"]["snsTopic"]'=\"${SNS_TOPIC}\" > amplify/team-provider-info_new.json
        # - rm amplify/team-provider-info.json
        # - mv amplify/team-provider-info_new.json amplify/team-provider-info.json
    build:
      commands:
        - '# Execute Amplify CLI with the helper script'
        - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
