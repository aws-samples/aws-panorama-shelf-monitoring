type shelfMonitor @model(timestamps:{updatedAt: "updatedOn"}) @key(fields: ["ProductType"]) {
  s3Uri: String
  count: Int
  ProductType: ProductType!
  Threshold: Int!
}

enum ProductType {
  BOTTLE
}